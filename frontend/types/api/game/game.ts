// User에서 Get할 때, playtimes에 들어있는 정보
export interface gameInUser {
  id: string;
  title: string;
  downloadUrl: string;
  executablePath: string;
  shortImgUrl: string;
  shortContent: string;
  iconUrl: string;
  createdAt: string;
  updatedAt: string;
  deletedAt: string;
}
