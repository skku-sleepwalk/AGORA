import { createStyles } from "@mantine/core";

export const usePostDetailViewerStyles = createStyles((theme) => ({
  postContainer: {
    width: 800,
    padding: 41,
  },

  content: {
    lineHeight: 1.5,
    padding: 0,
  },
}));
