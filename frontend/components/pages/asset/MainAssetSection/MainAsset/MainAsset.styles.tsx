import { createStyles } from "@mantine/core";

export const useMainAssetStyles = createStyles((theme) => ({
  wrapper: {
    width: "10rem",
    height: "16.9rem",
    marginBottom: "1rem",
    padding: "0.5rem",
  },

  button: {
    width: "1.6rem",
    height: "1.6rem",
    padding: "0.3rem",
  },
}));
