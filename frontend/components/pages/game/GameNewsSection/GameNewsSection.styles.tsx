import { createStyles } from "@mantine/core";

export const useGameNewsSectionStyles = createStyles((theme) => ({
  reviewSection: {
    width: "100%",
    height: "100%",
    padding: "2rem 1rem",
  },
}));
