import { createStyles } from "@mantine/core";

export const useGameBoardSectionStyles = createStyles((theme) => ({
  boardSection: {
    width: "100%",
    height: "100%",
    padding: "2rem 1rem",
  },

  writeButton: {
    padding: "0rem 0.7rem",
  },
}));
