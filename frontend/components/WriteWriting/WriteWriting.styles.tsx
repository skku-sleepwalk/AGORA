import { TextInput, createStyles } from "@mantine/core";

export const WriteWritingStyles = createStyles((theme) => ({
    container: {
        
        // backgroundColor: 'red'
      },
      TextInput:{
        
        width: 495
      }
   
}));
